#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../shared-access-signature/bin/generate-sas" "$@"
  ret=$?
else 
  node  "$basedir/../shared-access-signature/bin/generate-sas" "$@"
  ret=$?
fi
exit $ret
