copy files onto your linux box.

First you must modify storm-kafka-demo/config.xml, the field [Storage Account Name] and [Storage Account Key] changes to your storage account and key.

Run client.sh, node.sh(sudo) start_server_processes.sh, storm.sh  and point your browser to port 80 of the server url. 

